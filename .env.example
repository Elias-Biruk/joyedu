# Required secrets for docker-compose — copy this file to .env and fill in values
# Generate secure random values: openssl rand -base64 32

JWT_SECRET=
JWT_REFRESH_SECRET=
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d
